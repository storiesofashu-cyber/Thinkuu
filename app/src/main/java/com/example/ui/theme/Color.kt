package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Dark Teal Accent
val TealAccent = Color(0xFF00E5FF)

// Bento Grid Base Palettes - Forced to Deep Charcoal Black (#121212)
val BentoBg = Color(0xFF121212)
val BentoTextDark = Color(0xFFFFFFFF) // Crisp White for maximum readability

// Elevated Dark Gray (#1E1E1E) for all cards
val BentoNeutralCard = Color(0xFF1E1E1E)
val BentoBorder = Color(0xFF2C2C2C)
val BentoAccentPurple = Color(0xFF00E5FF) // Primary accent (Teal/Cyan)
val BentoTextGray = Color(0xFFB3B3B3) // Soft light gray

// Keep all specific Bento card variables mapping to Dark Mode counterparts for absolute consistency
val BentoPurpleCard = Color(0xFF1E1E1E)
val BentoPurpleText = Color(0xFFFFFFFF)
val BentoPurpleBadge = Color(0xFF1F2E30) // Dark teal base for badges

val BentoBlueCard = Color(0xFF1E1E1E)
val BentoBlueText = Color(0xFFFFFFFF)

val BentoRedCard = Color(0xFF1E1E1E)
val BentoRedText = Color(0xFFFFFFFF)
val BentoRedIndicator = Color(0xFFFF5252) // Bright coral red for high contrast exceptions/expenses

val BentoGreenCard = Color(0xFF1E1E1E)
val BentoGreenText = Color(0xFF66BB6A) // Beautiful emerald green for high contrast incomes/positives

// Theme M3 Fallback Overrides representing Dark Teal / Dark Slate
val Purple80 = Color(0xFF00E5FF)
val PurpleGrey80 = Color(0xFFB3E5FC)
val Pink80 = Color(0xFF80DEEA)

val Purple40 = Color(0xFF00838F)
val PurpleGrey40 = Color(0xFF006064)
val Pink40 = Color(0xFF004D40)
